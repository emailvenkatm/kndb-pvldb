*.o
*.bc
*.dylib
*.so
# pgxs regression output, at contrib/epistemic/results/; do NOT
# ignore contrib/epistemic/bench/results/ (that holds paper
# artefacts and is committed).
/results/
!bench/results/
# Dataset source clones are re-fetched via bench/datasets/normalize.py;
# repo commit hashes are pinned in each dataset's README. Only the
# normalized.jsonl output is committed.
bench/datasets/*/source/
regression.diffs
regression.out
tmp_check/
tmp_check_iso/
log/
output_iso/
